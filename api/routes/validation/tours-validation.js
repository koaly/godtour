const { body } = require('express-validator/check')

const config = {
    tour: [
        body('name')
            .isAlphanumeric()
            .trim()
            .escape()
            .withMessage('only alphabet and number can insert')
            .isLength({ min: 3 })
            .withMessage('require more than 3 charaters')
        , body('displayName')
            .isAlphanumeric()
            .trim()
            .escape()
            .withMessage('only alphabet and number can insert')
            .isLength({ min: 3 })
            .withMessage('require more than 3 charaters')
        , body('price')
            .isFloat({ min: 0 })
            .withMessage('require number more than or equar zero')
        , body('dest')
            .isAlpha()
            .withMessage('require alphabet')
        , body('dayDuration')
            .isNumeric({ min: 0 })
            .withMessage('require number')
        , body('nightDiration')
            .isNumeric({ min: 0 })
            .withMessage('require number')
        , body('startBookDate')
            //check date 23/10/2134 only allowed

            //p new i don't what format do you want to use
            //just new Date(23.10.2134)
            .matches(/^([0-9]{2})\/([0-9]{2})\/([0-9]{4})$/)
            .withMessage("require dd/mm/yyyy")
        , body('startBookTime')
            //time 12:00
            .matches(/^([0-9]{2})\:([0-9]{2})/)
            .withMessage("require time 10:00")
        , body('endBookDate')
            .matches(/^([0-9]{2})\/([0-9]{2})\/([0-9]{4})$/)
            .withMessage("require dd/mm/yyyy")
        , body('endBookTime')
            .matches(/^([0-9]{2})\:([0-9]{2})/)
            .withMessage("require time 10:00")
        , body('departDate')
            .matches(/^([0-9]{2})\/([0-9]{2})\/([0-9]{4})$/)
            .withMessage("require dd/mm/yyyy")
        , body('returnDate')
            .matches(/^([0-9]{2})\/([0-9]{2})\/([0-9]{4})$/)
            .withMessage("require dd/mm/yyyy")
        , body('seat')
            .isNumeric({ min: 0 })
            .withMessage('require number')
        , body('food')
            .isAlphanumeric()
            .trim()
            .escape()
            .withMessage('only alphabet and number can insert')
        , body('detail')
            .isAlphanumeric()
            .trim()
            .escape()
            .withMessage('only alphabet and number can insert')
        , body('highlight')
            .isAlphanumeric()
            .trim()
            .escape()
            .withMessage('only alphabet and number can insert')


    ]
}
module.exports = config